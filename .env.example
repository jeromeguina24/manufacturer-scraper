# HubSpot private-app access token.
# Create a private app in HubSpot (Settings -> Integrations -> Private Apps)
# with these scopes: content, files, crm.schemas.blog_posts.write (best effort),
# and optionally configuration. See docs/hubspot-setup.md.
HUBSPOT_ACCESS_TOKEN=
